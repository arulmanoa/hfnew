export enum EmployeeRequestStatus {
    Submitted = 100,
    Rejected = 200,
    Approved = 300
}

export enum CompensationDetailsStatus
    {
        Available = 100,
        Blocked = 200,
        Expired = 300,
        AvailmentRequested = 400,
        Availed = 500
    }